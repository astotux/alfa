import { ROUTES } from '@/shared/config/routes';
import { SquarePen } from 'lucide-react';

export const sidebarConfig = [
  {
    title: 'Новый чат',
    link: ROUTES.CHAT,
    icon: SquarePen,
  },
];
